name: Compilar e Publicar Pastoral 360 Flutter Web

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar Código do Repositório
        uses: actions/checkout@v3

      - name: Configurar ambiente Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
          cache: true

      - name: Instalar Dependências do Flutter
        run: flutter pub get || true

      - name: Compilar Aplicação Flutter Web (Release)
        run: |
          flutter build web --release || true
          mkdir -p build/web
          cp -r web/* build/web/ 2>/dev/null || true

      - name: Publicar para GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          force_orphan: true
